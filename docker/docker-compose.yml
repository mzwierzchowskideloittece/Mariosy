services:
  keycloak:
    image: quay.io/keycloak/keycloak:22.0.1
    env_file:
      - .env-keycloak
    ports:
      - "8080:8080"
    command:
      - start-dev
    volumes:
      - keycloak_data:/opt/keycloak/data/
volumes:
  keycloak_data: